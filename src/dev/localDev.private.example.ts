// Copy to localDev.private.ts (gitignored) for personal localhost cheats.

/** Committed stub — the real helper is gitignored and never deployed. */
export function installPrivateDevTools(): void {}
